Spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/user_customer?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: 112811
    #session失效时间
  session:
    timeout: 864000
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

##下面这些内容是为了让MyBatis映射
##指定Mybatis的Mapper文件
#mybatis.mapper-locations=classpath:mappers/*xml
##指定Mybatis的实体目录
#mybatis.type-aliases-package=com.example.mybatis.entity
## 应用服务 WEB 访问端口
server:
  port: 8080
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    global-config:
      db-config:
         logic-delete-field: isDelete # 全局逻辑删除字段名
         logic-delete-value: 1 # 逻辑已删除值
         logic-not-delete-value: 0 # 逻辑未删除值
